DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=tasiburada123
DB_NAME=tasiburada_dev
DB_POOL=10

NODE_ENV=development
PORT=3000
CORS_ORIGIN=https://tasiburada.com

# JWT Authentication
# JWT_SECRET=change_me_in_real_env

# SMTP (E-posta gönderimi)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=noreply@tasiburada.com
SMTP_PASS=your_smtp_password
SMTP_FROM="Taşıburada" <noreply@tasiburada.com>
FRONTEND_URL=http://localhost:5173

# Docker kullanıyorsan DB_PASSWORD docker-compose.yml'deki ile aynı olmalı
# Docker varsayılan şifre: tasiburada123